

## Plan: Remove Account Icon from Curate My Night Session

### Change

**`src/components/SessionStarter.tsx`** — Remove the `User` icon button (the account icon at `absolute right-6 top-6`) and the `User` import from `lucide-react`. The account icon remains accessible on the landing page.

